# Turso credentials for the /api/sessions function.
# Get these from `turso db show claude-sessions` and `turso db tokens create claude-sessions`.
# In production set them with: vercel env add TURSO_DATABASE_URL
TURSO_DATABASE_URL=libsql://your-db.turso.io
TURSO_AUTH_TOKEN=your-token
